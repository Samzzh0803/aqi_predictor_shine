pandas>=2.1
numpy>=1.26
requests>=2.31
scikit-learn>=1.3
tensorflow>=2.13
hopsworks==5.0.6  # pinned exactly: ADR-011 documents version-specific behavior (HUDI format, statistics_config, Kafka timeout, join semantics); leaving this unpinned lets a routine `pip install` resolve a different build with different bugs
confluent-kafka<=2.3.0  # hsfs's Python engine buffers feature-group writes through Kafka; not always pulled in transitively
nest_asyncio  # hsfs imports this unconditionally at module load but some hopsworks builds don't declare it as a dependency
shap>=0.43
matplotlib>=3.8
fastapi>=0.104
uvicorn[standard]>=0.24
streamlit>=1.28
plotly>=5.18
joblib>=1.3
python-dotenv>=1.0
pydantic-settings>=2.0
pydantic>=2.4
tenacity>=8.2
pytest>=7.4
pytest-cov>=4.1
httpx>=0.25
ruff>=0.1
PyYAML>=6.0
pyarrow>=14.0
